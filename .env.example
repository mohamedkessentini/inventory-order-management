# Used by docker-compose.yml at the root. Copy to .env and adjust.
# Per-service local dev still uses server/.env.example.

JWT_SECRET=dev-secret-change-me
JWT_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:8091
API_URL=http://localhost:4100/api

MONGO_PORT=27019
API_PORT=4100
CLIENT_PORT=8091
